[project]
name = "local-llm-support-triage"
version = "0.2.0"
description = "Customer support ticket triage with Ollama, FastAPI, PostgreSQL, MongoDB, RabbitMQ, and MLflow."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"
